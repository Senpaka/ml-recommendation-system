[project]
name = "pet-recommendation"
version = "0.1.0"
description = "Project recommendation system with micro services architecture"
authors = [
    {name = "Senpaka"}
]
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
